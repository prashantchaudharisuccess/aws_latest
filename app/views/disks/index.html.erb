      <p id="notice"><%= notice %></p>
        <div id="page-wrapper">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header">Disk</h1>
                </div>
                <!-- /.col-lg-12 -->
            </div>
            <!-- /.row -->
            <div class="row">
                <div class="col-lg-12">
                    <div class="panel panel-default">
                        <div class="panel-heading">
                            Disk
                        </div>
                        <!-- /.panel-heading -->
                        <div class="panel-body">
                            <table width="100%" class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <thead>
                                    <tr>
                                        <th>Dsik</th>
                                        <th>Discription</th>
                                        <th>Actions</th>
                                    </tr>
                                </thead>
                                <tbody>
                                  <% @disks.each do |disk| %>
                                    <tr class="odd gradeX">
                                        <td><%= link_to disk.name, disk_file_upload_path(disk.id, disk.name) %></td>
                                        <td><%= disk.description %></td>
                                        <td>
                                        <%= link_to 'Edit', edit_disk_path(disk) %> |
                                        <%= link_to 'Destroy', disk, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                                    </tr>
                                  <% end %>
                                </tbody>
                            </table>
                           
                        </div>
                        <!-- /.panel-body -->
                    </div>
                    <!-- /.panel -->
                    <%= link_to 'New Disk', new_disk_path, class: "btn btn-success" %>
                </div>
                <!-- /.col-lg-12 -->
            </div>
        </div>